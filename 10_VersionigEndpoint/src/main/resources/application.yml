spring:
  application:
    name: rest-with-spring
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring?useTimezone=true&serverTimezone=UTC
    username: root
    password: 'vitor468:'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show-sql: false
        open-in-view: false
logging:
  level:
    #root: WARN  aplica o nivel de log de forma global
    br.con.vitorramires468: DEBUG