cors:
  originPatterns: http://localhost:8080,http://www.erudio.com.br, http://localhost:3000
spring:
  application:
    name: rest-with-spring
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring?useTimezone=true&serverTimezone=UTC
    username: root
    password: 'vitor468:'
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show-sql: false
        open-in-view: false
logging:
  level:
    #root: WARN  aplica o nivel de log de forma global
    br.con.vitorramires468: DEBUG

spring.doc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true